# Manufacturer binaries: kept locally for interoperability analysis, never redistributed.
vendor/

# Packet captures may contain device serials and pairing keys.
captures/

# APK and decompiler output
*.apk
*.dex
jadx-out/

# Default output of tools/report.py. Testers run it inside a clone, and it
# names their controller, so it should never be committed by accident.
controller-report.txt

__pycache__/
*.py[cod]
.venv/
venv/
*-debug.spec
app.spec
build/
dist/
*.egg-info/
*.egg-link
.eggs/

.vscode/
.idea/
